/*I learned we can divide up our code into reusable parts called 'functions'. You can call or invoke this function like this: functionName();
Each time the function is called it will print out the message on the dev console. All of the code between the curly braces will be executed every time the function is called. */

// Example
function ourReusableFunction() {
  console.log("Heyya, World");
}

ourReusableFunction();

// Only change code below this line
function reusableFunction () {
    console.log("Hi World");
}
reusableFunction();
