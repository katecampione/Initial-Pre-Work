// I learned that string values are immutable meaning they cannot be changed. The only way to change the string would be to assign the variable a new string.

// Setup
var myStr = "Jello World";

// Only change code below this line

myStr = "Hello World";
