// I learned that to import a default export, you need to use a different import syntax. Unlike exported values, the primary method of importing a default export is to simply write the value's name after import.

"use strict";
import subtract from "math_functions";
subtract(7,4);
